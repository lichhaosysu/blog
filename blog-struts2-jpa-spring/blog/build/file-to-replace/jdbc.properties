# Properties file with JDBC-related settings.
# Applied by PropertyPlaceholderConfigurer from "dataSource.xml".
# Targeted at system administrators, to avoid touching the context XML files.

jdbc.url=jdbc:mysql://localhost:3306/jsp201338db0?useUnicode=true&characterEncoding=UTF-8
jdbc.username=jsp201338db0
jdbc.password=58296640
jdbc.validationQuery=select 'x'